
#BACKEND

routes/ → define endpoints (/login, /produtos)
controllers/ → lógica (o que acontece quando chama a rota)
models/ → conexão com banco
config/ → configs (db, jwt, etc)
server.js → inicia tudo